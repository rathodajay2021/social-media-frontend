import { Box } from '@mui/material';
import styled from 'styled-components';

export const ChatWrapper = styled(Box)`
    height: 100%;
    width: 100%;
    
    .text-msg-wrapper {
        width: 100%;
        margin: auto 0 0 0;
    }
`;
